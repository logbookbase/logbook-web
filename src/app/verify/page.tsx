import VerifyView from "@/components/VerifyView";

export const metadata = {
  title: "Verify · logbook",
  description: "Verify any logbook event id. Public, free, no login required.",
};

export default function VerifyPage() {
  return <VerifyView />;
}
